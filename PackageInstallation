def servicename = env.service_name
def servername = env.server_name


pipeline{
    agent {label 'jenkinsnodes'}
    stages{
        stage("Service"){
            steps{
                sh "ssh node1@${servername} 'sudo systemctl status $servicename ; exit 0 '"
            }
        }
    }
}
